# NUS-Fintech-Asset-Recommender
Asset Recommendation Project under NUS Fintech

## To run the app

1. Make sure you have your `.env` file set up
2. Run `docker compose up -d` to start the database in a docker container
2. From root, run `gradio src/app/app.py`